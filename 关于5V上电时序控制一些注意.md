	在逐飞给的原理图设计中，SY6280这颗芯片的EN引脚，（EN引脚作用：开关控制。拉高电平以使能集成电路。请勿悬空。）连接到了bb7的CR端口，我不知道接CR口怎么控制上电时序。
	目前我查到的，且gpt给出的建议是：将EN接普通GPIO。

给出示例：
GPIO初始化：
GPIO_SetLow(EXT5V_EN);
GPIO_Mode_Out_PP(EXT5V_EN);
初始化完成后：
delay_ms(50);
GPIO_SetHigh(EXT5V_EN);